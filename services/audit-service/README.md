# WCAG AI Copilot — Audit Service

This microservice handles the WCAG accessibility audit engine, including the LangGraph agent pipeline, dense/sparse hybrid retrieval from Qdrant, OpenAI-compatible embedding generation with Redis caching, and audit persistence.
