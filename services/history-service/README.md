# WCAG AI Copilot — History Service

This microservice aggregates past user chat conversations and accessibility audits. It reads directly from both the `wcag_conversations` and `wcag_audits` databases.
